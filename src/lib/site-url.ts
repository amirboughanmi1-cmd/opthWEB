/**
 * Canonical site origin — used by metadataBase, sitemap.xml and robots.txt.
 * Override per environment with NEXT_PUBLIC_SITE_URL (e.g. Vercel previews).
 */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://ophtahealth.com";
